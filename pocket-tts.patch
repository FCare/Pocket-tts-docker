diff --git a/pocket_tts/main.py b/pocket_tts/main.py
index c040425..f3431da 100644
--- a/pocket_tts/main.py
+++ b/pocket_tts/main.py
@@ -4,6 +4,7 @@ import os
 import sys
 import tempfile
 import threading
+import time
 from pathlib import Path
 from queue import Queue
 
@@ -40,8 +41,11 @@ cli_app = typer.Typer(
 # The pocket-tts server implementation
 # ------------------------------------------------------
 
-# Global model instance
+# Global model instance and inference lock (shared across all request threads)
 tts_model: TTSModel | None = None
+_inference_lock = threading.Lock()
+
+VOICES_STORE_DIR = Path("/root/.cache/pocket_tts/voices")
 
 web_app = FastAPI(
     title="Kyutai Pocket TTS API", description="Text-to-Speech generation API", version="1.0.0"
@@ -77,6 +81,44 @@ async def health():
     return {"status": "healthy"}
 
 
+@web_app.get("/voices")
+def list_voices():
+    """List stored voice embeddings."""
+    VOICES_STORE_DIR.mkdir(parents=True, exist_ok=True)
+    voices = [p.stem for p in sorted(VOICES_STORE_DIR.glob("*.safetensors"))]
+    return {"voices": voices}
+
+
+@web_app.post("/voices")
+def upload_voice(
+    name: str = Form(...),
+    voice_wav: UploadFile = File(...),
+):
+    """
+    Convert an uploaded audio file to a cached voice embedding (.safetensors).
+    The stored voice can then be used in /tts via voice_url=<name>.
+    """
+    if not name.strip() or "/" in name or "\\" in name:
+        raise HTTPException(status_code=400, detail="Invalid voice name")
+
+    VOICES_STORE_DIR.mkdir(parents=True, exist_ok=True)
+    dest = VOICES_STORE_DIR / f"{name}.safetensors"
+
+    suffix = Path(voice_wav.filename).suffix if voice_wav.filename else ".wav"
+    with tempfile.NamedTemporaryFile(delete=False, suffix=suffix) as tmp:
+        tmp.write(voice_wav.file.read())
+        tmp_path = tmp.name
+
+    try:
+        with _inference_lock:
+            model_state = tts_model.get_state_for_audio_prompt(Path(tmp_path), truncate=True)
+        export_model_state(model_state, dest)
+    finally:
+        os.unlink(tmp_path)
+
+    return {"name": name, "path": str(dest)}
+
+
 def write_to_queue(queue, text_to_generate, model_state):
     """Allows writing to the StreamingResponse as if it were a file."""
 
@@ -93,30 +135,44 @@ def write_to_queue(queue, text_to_generate, model_state):
         def close(self):
             self.queue.put(None)
 
-    audio_chunks = tts_model.generate_audio_stream(
-        model_state=model_state, text_to_generate=text_to_generate
-    )
-    stream_audio_chunks(FileLikeToQueue(queue), audio_chunks, tts_model.config.mimi.sample_rate)
+    with _inference_lock:
+        audio_chunks = tts_model.generate_audio_stream(
+            model_state=model_state, text_to_generate=text_to_generate
+        )
+        stream_audio_chunks(FileLikeToQueue(queue), audio_chunks, tts_model.config.mimi.sample_rate)
 
 
-def generate_data_with_state(text_to_generate: str, model_state: dict):
+def generate_data_with_state(text_to_generate: str, model_state: dict, t_start: float):
     queue = Queue()
+    t_first: float | None = None
+    total_bytes = 0
 
-    # Run your function in a thread
     thread = threading.Thread(target=write_to_queue, args=(queue, text_to_generate, model_state))
     thread.start()
 
-    # Yield data as it becomes available
-    i = 0
     while True:
         data = queue.get()
         if data is None:
             break
-        i += 1
+        if t_first is None:
+            t_first = time.monotonic()
+        total_bytes += len(data)
         yield data
 
     thread.join()
 
+    t_end = time.monotonic()
+    gen_time = t_end - t_start
+    ttft = (t_first - t_start) if t_first is not None else gen_time
+    # WAV output: mono, 16-bit PCM → 2 bytes/sample; subtract 44-byte header
+    sample_rate = tts_model.config.mimi.sample_rate
+    audio_duration = max(0, total_bytes - 44) / (sample_rate * 2)
+    rtf = gen_time / audio_duration if audio_duration > 0 else float("inf")
+    logging.warning(
+        "TTS done | TTFT: %.2fs | audio: %.2fs | total: %.2fs | RTF: %.3f",
+        ttft, audio_duration, gen_time, rtf,
+    )
+
 
 @web_app.post("/tts")
 def text_to_speech(
@@ -132,6 +188,7 @@ def text_to_speech(
         voice_url: Optional built-in voice name (e.g., "alba"), or voice URL (http://, https://, or hf://)
         voice_wav: Optional uploaded voice file (mutually exclusive with voice_url)
     """
+    t_start = time.monotonic()
     if not text.strip():
         raise HTTPException(status_code=400, detail="Text cannot be empty")
 
@@ -143,17 +200,23 @@ def text_to_speech(
 
     # Use the appropriate model state
     if voice_url is not None:
-        if not (
+        stored = VOICES_STORE_DIR / f"{voice_url}.safetensors"
+        if stored.exists():
+            model_state = tts_model._cached_get_state_for_audio_prompt(str(stored))
+            logging.warning("Using stored voice: %s", voice_url)
+        elif not (
             voice_url.startswith("http://")
             or voice_url.startswith("https://")
             or voice_url.startswith("hf://")
             or voice_url in _ORIGINS_OF_PREDEFINED_VOICES
         ):
             raise HTTPException(
-                status_code=400, detail="voice_url must start with http://, https://, or hf://"
+                status_code=400,
+                detail=f"Unknown voice '{voice_url}'. Use a URL, a predefined name, or upload via POST /voices.",
             )
-        model_state = tts_model._cached_get_state_for_audio_prompt(voice_url)
-        logging.warning("Using voice from URL: %s", voice_url)
+        else:
+            model_state = tts_model._cached_get_state_for_audio_prompt(voice_url)
+            logging.warning("Using voice from URL: %s", voice_url)
     elif voice_wav is not None:
         # Use uploaded voice file - preserve extension for format detection
         suffix = Path(voice_wav.filename).suffix if voice_wav.filename else ".wav"
@@ -172,7 +235,7 @@ def text_to_speech(
         raise HTTPException(status_code=500, detail="This should never happen.")
 
     return StreamingResponse(
-        generate_data_with_state(text, model_state),
+        generate_data_with_state(text, model_state, t_start),
         media_type="audio/wav",
         headers={
             "Content-Disposition": "attachment; filename=generated_speech.wav",
@@ -205,11 +268,15 @@ def serve(
     quantize: Annotated[
         bool, typer.Option(help="Apply int8 quantization to reduce memory usage")
     ] = False,
+    device: Annotated[
+        str, typer.Option(help="Force torch device")
+    ] = "cpu",
 ):
     """Start the FastAPI server."""
 
     global tts_model
-    tts_model = TTSModel.load_model(language=language, config=config, quantize=quantize)
+    tts_model = TTSModel.load_model(language=language, config=config, quantize=quantize, device=device)
+    print(f"Model running on: {tts_model.device}")
 
     uvicorn.run("pocket_tts.main:web_app", host=host, port=port, reload=reload)
 
diff --git a/pocket_tts/models/tts_model.py b/pocket_tts/models/tts_model.py
index 04718d0..2aedbba 100644
--- a/pocket_tts/models/tts_model.py
+++ b/pocket_tts/models/tts_model.py
@@ -135,6 +135,7 @@ class TTSModel(nn.Module):
         noise_clamp: float | None,
         eos_threshold,
         origin: Path | None = None,
+        device: str | None = None,
     ) -> Self:
         tts_model = cls._from_pydantic_config(
             config, temp, lsd_decode_steps, noise_clamp, eos_threshold, origin=origin
@@ -155,7 +156,7 @@ class TTSModel(nn.Module):
                 )
             logger.info(f"Loading FlowLM weights from {config.flow_lm.weights_path}")
             state_dict_flowlm = get_flow_lm_state_dict(
-                download_if_necessary(config.flow_lm.weights_path)
+                download_if_necessary(config.flow_lm.weights_path), device
             )
             tts_model.flow_lm.load_state_dict(state_dict_flowlm, strict=True)
 
@@ -183,7 +184,7 @@ class TTSModel(nn.Module):
             outer_dim=mimi_config["outer_dim"],
             encoder_transformer=encoder_transformer,
             decoder_transformer=decoder_transformer,
-        ).to(device="cpu")
+        ).to(device=device)
 
         # Load mimi weights from the config safetensors file with complete mapping for strict loading
 
@@ -193,7 +194,7 @@ class TTSModel(nn.Module):
                     "If you specify mimi.weights_path you should specify flow_lm.weights_path"
                 )
             logger.info(f"Loading Mimi weights from {config.mimi.weights_path}")
-            mimi_state = get_mimi_state_dict(download_if_necessary(config.mimi.weights_path))
+            mimi_state = get_mimi_state_dict(download_if_necessary(config.mimi.weights_path), device=device)
             tts_model.mimi.load_state_dict(mimi_state, strict=True)
 
         tts_model.mimi.eval()
@@ -227,7 +228,7 @@ class TTSModel(nn.Module):
                     continue
                 module._module_absolute_name = module_name
 
-        return tts_model
+        return tts_model.to(device = device)
 
     @classmethod
     def load_model(
@@ -239,6 +240,7 @@ class TTSModel(nn.Module):
         noise_clamp: float | int | None = DEFAULT_NOISE_CLAMP,
         eos_threshold: float = DEFAULT_EOS_THRESHOLD,
         quantize: bool = False,
+        device: str = "cpu"
     ) -> Self:
         """Load a pre-trained TTS model with specified configuration.
 
@@ -306,7 +308,7 @@ class TTSModel(nn.Module):
         logger.info(f"Loading model from config at {config_path}...")
 
         tts_model = TTSModel._from_pydantic_config_with_weights(
-            config, temp, lsd_decode_steps, noise_clamp, eos_threshold, origin=config_path
+            config, temp, lsd_decode_steps, noise_clamp, eos_threshold, origin=config_path, device = device
         )
 
         if quantize:
@@ -778,7 +780,7 @@ class TTSModel(nn.Module):
         latents_queue.put(None)
         logger.info("Average generation step time: %d ms", int(statistics.mean(steps_times)))
 
-    @lru_cache(maxsize=2)
+    @lru_cache(maxsize=int(os.environ.get("POCKET_TTS_VOICE_CACHE_SIZE", 2)))
     def _cached_get_state_for_audio_prompt(
         self, audio_conditioning: Path | str | torch.Tensor, truncate: bool = False
     ) -> dict:
diff --git a/pocket_tts/utils/weights_loading.py b/pocket_tts/utils/weights_loading.py
index 3f41bda..4a552ca 100644
--- a/pocket_tts/utils/weights_loading.py
+++ b/pocket_tts/utils/weights_loading.py
@@ -4,9 +4,9 @@ import safetensors
 import torch
 
 
-def get_flow_lm_state_dict(path: Path) -> dict:
+def get_flow_lm_state_dict(path: Path, device: str) -> dict:
     state_dict = {}
-    with safetensors.safe_open(path, framework="pt", device="cpu") as f:
+    with safetensors.safe_open(path, framework="pt", device=device) as f:
         for key in f.keys():
             if (
                 key.startswith("flow.w_s_t.")
@@ -30,9 +30,9 @@ def get_flow_lm_state_dict(path: Path) -> dict:
     return state_dict
 
 
-def get_mimi_state_dict(path: Path) -> dict:
+def get_mimi_state_dict(path: Path, device = str) -> dict:
     state_dict = {}
-    with safetensors.safe_open(path, framework="pt", device="cpu") as f:
+    with safetensors.safe_open(path, framework="pt", device=device) as f:
         for key in f.keys():
             if (
                 key.startswith("model.quantizer.vq.")
